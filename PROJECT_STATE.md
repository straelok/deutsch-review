# Project state

## Текущее состояние

- Подготовлены проектные инструкции и исходная спецификация.
- Создан минимальный Flutter-каркас для Android и Windows.
- Добавлены тёмная тема, адаптивная навигация `Heute` / `Lernen` / `Wörter`
  и smoke-тест навигации.
- Установлен Git for Windows 2.55.0.windows.5.
- Установлены Flutter 3.47.5 stable и Dart 3.13.4.
- `flutter doctor -v` подтверждает Windows 11, доступные устройства Windows,
  Chrome и Edge, а также доступность сетевых ресурсов.
- Установлена Visual Studio Community 2026 18.10.2 с Windows C++ toolchain;
  Flutter успешно распознаёт Windows SDK 10.0.26100.0.
- Установлены Android Studio, Android SDK 37, Android Platform 36 и NDK
  28.2.13676358; все Android-лицензии приняты.
- Платформенные Android/Windows-файлы заново созданы штатным генератором
  Flutter 3.47.5.
- Форматирование выполнено; `flutter analyze` и smoke-тест проходят.
- Успешно собраны отладочные версии Windows и Android APK.
- Создан Git-репозиторий с веткой `main`, связанный с публичным репозиторием
  `https://github.com/straelok/deutsch-review`.
- Автор локальных коммитов настроен через приватный GitHub `noreply` email;
  проверенный каркас зафиксирован базовым коммитом и отправлен в `origin/main`.
- Постоянный идентификатор Android-приложения —
  `io.github.straelok.deutschreview`.
- Определены доменные модели учебного элемента, расписания и неизменяемого
  события повторения; добавлены тесты основных инвариантов.
- Спроектированы схема SQLite версии 1 и стратегия последовательных
  транзакционных миграций в `docs/DATABASE_SCHEMA.md`.
- Выбрана `sqlite3` 3.6.0 (MIT, Android и Windows), реализованы открытие базы,
  миграция `0 -> 1` и проверки целостности.
- Добавлены абстракция репозитория учебных элементов, SQLite-реализация,
  мягкое удаление и тесты сохранения с повторным открытием файла.
- Реализован адаптивный интерфейс раздела `Wörter`: список, добавление и
  редактирование слов и существительных, проверка обязательных полей и отметка
  изученного материала.
- Файловая SQLite-база подключена через `path_provider` 2.1.6 и хранится в
  системном каталоге поддержки приложения на Android и Windows.
- Создан и проверен локальный Windows-комплект в `build/windows/x64/install` и
  архив `dist/Worttrieb-Windows-Debug.zip` для ручного тестирования.
- `flutter analyze` и 12 тестов проходят; отладочные сборки Windows и Android
  успешно созданы, Windows-процесс из полного комплекта запускается и отвечает.
- Этап 2.1 реализован сквозным UI: переключение немецкого/русского языка,
  поиск, защита от дубликатов, мягкое удаление с отменой, базовые повторения и
  статистика проблемных слов.
- Форма добавления сокращена до слова, перевода и полей существительного;
  карточка показывает неизменяемую дату добавления, а все активные элементы
  автоматически доступны для повторения.
- Схема SQLite обновлена до версии 2. Настройки языка и неизменяемые попытки
  сохраняются локально; перед миграцией версии 1 создаётся резервная копия.
- `flutter analyze` и 20 тестов проходят; новые отладочные сборки Windows и
  Android успешно созданы, Windows-билд проверен на существующем профиле.
- В материал добавлены необязательные заметка и пример использования. Они
  доступны в форме и поиске, видны в карточке и показываются после ответа при
  повторении; пустые значения не сохраняются.
- Раздел словаря переименован в `Wörter` / `Слова`, из счётчика убрана
  привязка к курсу DAA. В редактировании показывается статистика выбранного
  слова. Для приложения создана новая иконка Android и Windows.
- Обновлённый APK `Worttrieb` установлен через ADB на Motorola Edge 40 Neo с
  сохранением постоянного Android application ID и локальных данных.
- Подэтап 3.1 завершён: схема SQLite версии 3 создаёт пять обязательных дневных
  занятий по 20 ответов, сохраняет очередь и прогресс, а результат ответа и
  состояние занятия записываются одной транзакцией.
- Старые неиспользуемые таблицы и доменные модели FSRS удалены миграцией после
  автоматической резервной копии. Приоритет слов рассчитывается по последним
  десяти ответам; UI `Heute`, `Lernen` и `Statistik` обновлён.
- `flutter analyze` проходит без замечаний, все 25 тестов успешны.
- Для подэтапа 3.1.5 реализованы клиент синхронизации, объединение снимков
  SQLite, ввод и хранение нормализованного ника, автоматический повтор после
  локальных изменений и восстановления сети, ручная кнопка и статус в UI.
- В бесплатном проекте Supabase `hjihhrcxkpzglgfmfuzs` применена миграция с
  закрытым прямым доступом и развёрнута Edge Function `sync-profile`, которая
  принимает только публичный ключ и точный ник. Секретный ключ остаётся только
  на стороне Supabase.
- Используются `supabase` 2.16.1 и `connectivity_plus` 7.3.1; обе зависимости
  поддерживают Android и Windows и имеют permissive-лицензии. Анализатор и все
  29 тестов проходят; debug-сборки Windows и Android создаются успешно.
- Windows и Android повторно собраны с публичной конфигурацией Supabase через
  локальный исключённый из Git файл `tool/supabase_defines.json`; Windows-билд
  запущен. Реальный обмен между Windows и Android по одному нику подтверждён
  пользователем; ручная проверка офлайн-повтора отложена.
- Подэтап 3.2 реализован вместе с UI: Android запрашивает разрешения по кнопке
  в `Heute`, планирует напоминания на 13:30, 16:30 и 19:30 по часовому поясу
  устройства, отменяет остаток текущего дня после пяти занятий и открывает
  `Heute` по нажатию на уведомление.
- Используются `flutter_local_notifications` 22.3.1 (BSD-3-Clause),
  `flutter_timezone` 5.1.0 (Apache-2.0) и `timezone` 0.11.1 (BSD-2-Clause).
  Анализатор и все 31 тест проходят; Android debug APK успешно собран.
- APK этапа 3.2 установлен поверх существующей версии на Motorola Edge 40 Neo
  без удаления локальных данных; приложение успешно запущено через ADB.
- В этапе 3.3 разделы `Heute` и `Lernen` объединены: дневной план, запуск,
  продолжение и само упражнение находятся на одном экране. Из упражнения можно
  вернуться к плану без потери сохранённой очереди.
- Завершённые обязательные занятия больше не создают дополнительные занятия по
  отдельности. После выполнения всех пяти появляется явная кнопка «Получить
  новый урок», создающая и сразу запускающая дополнительное занятие.
- Анализатор и все 32 теста проходят. Debug-сборки Android и Windows созданы;
  новый APK установлен и запущен на Motorola Edge 40 Neo с сохранением данных.
- Этап 4 сокращён до импорта и экспорта слов через JSON. Реализованы формат
  версии 1, системные файловые диалоги Android/Windows, полная проверка до
  записи, предварительный итог, пропуск дублей и транзакционное сохранение.
- Для системных файловых диалогов используется `file_picker` 13.1.0 (MIT) с
  поддержкой Android и Windows. Анализатор и все 36 тестов проходят; обе
  debug-сборки созданы, APK установлен и запущен на Motorola Edge 40 Neo.
- Подэтап 5.0 завершён на уровне документации: согласованы четвёртый раздел
  грамматики, два ежедневных занятия по 10 заданий, базовые темы, отдельные
  темы `sein` и `haben`, а также программа Hueber *Schritte plus Neu 1* A1.1.
  Код приложения и схема данных на этом подэтапе не изменялись.
- Подэтап 5.1 реализован сквозным сценарием: добавлен четвёртый раздел
  `Grammatik` / `Грамматика`, двуязычные страницы тем, отметка изученности и
  статистика. В словаре появился тип `verb`, включая UI, поиск, JSON версии 2
  и синхронизацию.
- Добавлены справочные темы о глаголе, инфинитиве и личных местоимениях, а
  также обучаемые темы регулярного Präsens, `sein` и `haben`. Тема доступна
  только при наличии поддерживаемого активного глагола в словаре.
- Встроенный статический банк содержит ровно 1000 оригинальных предложений для
  25 регулярных глаголов, 80 заданий для `sein` и 80 для `haben`. Во время
  занятия приложение выбирает готовые записи и не генерирует предложения.
- Изученная доступная тема расширяет дневной план занятиями 6 и 7 по 10
  заданий. Первое задание проверяет таблицу форм одного глагола, задания 2–10 —
  готовые предложения; очередь и прогресс точно восстанавливаются.
- Схема SQLite обновлена до версии 4 с резервной копией и сохранением старых
  занятий. Грамматические отметки, неизменяемые попытки и тип занятия включены
  в снимок синхронизации версии 2; миграция и Edge Function развёрнуты в
  Supabase.
- `flutter analyze` проходит без замечаний, все 44 теста успешны. Debug-сборки
  Windows и Android созданы с конфигурацией Supabase; APK установлен и запущен
  на Motorola Edge 40 Neo, Windows-сборка также проверена запуском.
- После повторной ошибки запуска отдельного Windows `.exe` добавлен обязательный
  скрипт `tool/package_windows.ps1`. Он собирает, копирует полный комплект,
  проверяет Flutter/SQLite DLL и ресурсы и только затем создаёт ZIP; одиночный
  файл из `runner/Debug` больше не считается пользовательской сборкой.
- Новый архив `dist/Worttrieb-Windows-Debug.zip` проверен: внутри находятся
  семь DLL и Flutter-ресурсы. Приложение успешно запущено именно из полного
  каталога `dist/Worttrieb-Windows-Debug`.
- Подэтап 5.2 реализован: все 16 базовых и ранее добавленных грамматических тем
  стали обучаемыми. Добавлены части речи, существительные и артикли,
  прилагательные и наречия, предлоги, простой порядок слов, Ja/Nein- и
  W-вопросы, `nicht` / `kein`, а также введение в Nominativ и Akkusativ.
- Банк грамматики версии 2 содержит 1585 статических заданий. Кроме текстового
  ввода поддерживаются выбор варианта, `Ja` / `Nein` и сборка предложения из
  слов с отменой последнего шага. Для каждой темы есть не менее 20 заданий;
  доступность фильтруется по типу и лемме активного словарного материала.
- Занятия 6 и 7 теперь смешивают все доступные изученные темы и применяют
  прежний вес последних десяти ответов. Стабильные ID сохраняют точную очередь
  при перезапуске и позволяют синхронизировать её без изменения схемы SQLite
  или облачного формата.
- `flutter analyze` проходит без замечаний, все 46 тестов успешны. Полный
  Windows-комплект и Android APK собраны с Supabase, Windows-комплект проверен
  запуском, APK установлен и запущен на Motorola Edge 40 Neo.
- Пользователь подтвердил ручную проверку Android и Windows: импорт и экспорт
  JSON, основной сценарий занятий, синхронизация, работа без сети и повторная
  синхронизация после восстановления сети работают исправно. Актуальные сборки
  также проверены пользователем.

## Принятые решения

- Flutter/Dart с поддержкой Android и Windows.
- SQLite и локальная база как основной источник данных.
- Offline-first; синхронизация Supabase по нику входит в этап 3, но не нужна для
  основной локальной работы.
- Учебные данные добавляются и редактируются вручную либо импортируются из
  минимального JSON; активные слова можно экспортировать в тот же формат.
- CSV, импорт из буфера и отдельная система пользовательских резервных копий
  не разрабатываются. Синхронизация устройств остаётся на Supabase.
- FSRS и рейтинги ответов полностью исключены. Приоритет определяется долей
  правильных среди последних десяти ответов по простой весовой формуле.
- Подробные требования хранятся в `PRODUCT_SPEC.md`, рабочие правила — в `AGENTS.md`.
- Название приложения — `Worttrieb`: ассоциация с ростом словарного запаса и
  внутренней тягой к изучению языка.
- Постоянный идентификатор Android-приложения —
  `io.github.straelok.deutschreview`.
- Общие поля учебного элемента хранятся в столбцах SQLite, различающееся
  содержимое — в валидном JSON.
- На локальный день создаются пять занятий по 20 ответов. Прогресс и очередь
  незавершённого занятия сохраняются для продолжения после перезапуска.
- Версия схемы хранится в `PRAGMA user_version`; миграции выполняются только
  вперёд, по одной версии и в транзакции, без автоматического сброса данных.
- Число новых слов не ограничивается приложением и следует программе DAA.
- Проблемные слова получают больший вес по последним десяти ответам; слово без
  истории считается имеющим 0% успешности.
- Android напоминает о незавершённых занятиях в 13:30, 16:30 и 19:30 по
  локальному времени устройства.
- Синхронизация использует только нормализованный ник без почты и пароля. Знание
  ника даёт полный доступ к профилю; пользователь явно принял этот риск.
- Очередь повторений не блокирует остальные функции приложения.
- После этапа интерфейса словаря предоставляется запускаемая Windows-сборка для
  ручной проверки до продолжения разработки.
- После завершения всей разработки нужно напомнить пользователю отключить
  Windows Developer Mode, включённый для сборки Flutter-плагинов.
- Каждая новая пользовательская функция реализуется вместе с доступным UI в
  том же этапе; наличие только backend-кода не считается завершением функции.
- Форма материала не содержит уровень, урок, тему, источник и отметку
  изученности. Любое добавленное слово сразу участвует в повторениях; UI
  показывает неизменяемую дату добавления.
- Грамматические темы являются отдельным видом материала: пользователь явно
  отмечает тему изученной. Задание доступно только при изученной теме и наличии
  активного слова подходящего типа в словаре.
- Грамматический контент следует последовательности стандартного издания
  Hueber *Schritte plus Neu 1* A1.1, но объяснения и предложения создаются
  специально для приложения. Пользовательские примеры слов не преобразуются в
  автоматически проверяемые задания.
- После появления доступной изученной грамматической темы дневной план
  расширяется занятиями 6 и 7 по 10 заданий. `sein` и `haben` реализованы как
  отдельные темы с вводом полной словоформы.
- Первый банк регулярного Präsens ограничен 25 поддерживаемыми глаголами, но
  содержит 1000 заранее подготовленных предложений. Наличие другого глагола в
  словаре само по себе не создаёт для него упражнений.
- Дальнейшее последовательное заполнение программы *Schritte plus Neu 1* A1.1
  отложено. Новые грамматические темы добавляются только по мере их фактического
  прохождения пользователем в школе и после отдельного запроса.

## Открытые решения

- `flutter_timezone` 5.1.0 пока вызывает предупреждение Flutter о старом
  способе подключения Kotlin Gradle Plugin; текущую Android-сборку это не
  блокирует, но совместимость нужно перепроверить при обновлении Flutter.

## Следующий шаг

Активных незавершённых задач нет. Новые темы курса не начинать без сообщения
пользователя о пройденном в школе материале; следующую функцию приложения
выбирать отдельным запросом.
