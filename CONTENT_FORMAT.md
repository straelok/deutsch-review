# Формат JSON слов Worttrieb

Текущая версия — `1`. Формат предназначен только для слов и существительных.

## Минимальный файл

```json
{
  "format_version": 1,
  "words": [
    {
      "type": "word",
      "german": "lernen",
      "translation_ru": "учить"
    },
    {
      "type": "noun",
      "german": "Tisch",
      "translation_ru": "стол",
      "article": "der",
      "plural": "Tische"
    }
  ]
}
```

## Поля

- `format_version` — обязательное число `1`.
- `words` — обязательный массив.
- `type` — `word` или `noun`.
- `german` и `translation_ru` — обязательные непустые строки.
- Для `noun` обязательны `article` (`der`, `die` или `das`) и `plural`.
- `example` и `note` — необязательные строки.
- `id`, `created_at` и `updated_at` необязательны при импорте и добавляются при
  экспорте. Даты используют ISO 8601.

## Безопасность импорта

Весь файл проверяется до записи. Неизвестная версия или неверный элемент
отклоняют весь пакет. Совпадающие ID и дубли одного типа с одинаковым немецким
написанием пропускаются. Перед сохранением приложение показывает число новых и
пропущенных слов; новые элементы записываются одной транзакцией.
